<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_brandico_github"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_brandico_github"
        android:supportsRtl="true"
        android:theme="@style/Theme.Github">
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Github.splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.setting.SettingActivity"
            android:exported="true"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/Theme.Github.sf" />
        <activity
            android:name=".ui.favorite.FavoriteActivity"
            android:exported="true"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/Theme.Github.sf" />
        <activity
            android:name=".ui.detail.DetailActivity"
            android:exported="true"
            android:theme="@style/Theme.Github.Grey" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true" />
    </application>

</manifest>